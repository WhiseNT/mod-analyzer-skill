# 模组代码分析报告模板

> 本模板面向模组开发者、整合包魔改作者（KubeJS/CraftTweaker），从代码层面梳理 mod 的架构、关键实现、mixin 影响和魔改点。使用前必须完成阶段 1（静态清单）和阶段 2（反编译/源码读取）。

# 《[Mod 名称]》模组代码分析报告

## 0. 一句话结论

用 2-4 句话说明：这个 mod 的代码架构质量如何、哪些部分容易魔改、哪些部分有兼容风险、总体可魔改友好度。

## 1. 基础信息与分析范围

| 项目 | 内容 |
|------|------|
| Mod 名称 | |
| Mod ID | |
| 版本 | |
| Minecraft 版本 | |
| Loader | Forge / NeoForge / Fabric / Quilt |
| 混淆映射 | MCP / Yarn / Mojmap / 其他 |
| 反编译状态 | 成功 / 部分成功 / 未反编译 |
| 源码可用 | 是 / 部分 / 否 |
| 分析日期 | |

## 2. 注册架构

### 2.1 注册方式

| 注册目标 | 注册方式 | 位置（类/方法） | 特点 |
|----------|----------|-----------------|------|
| Items | DeferredRegister / Registry.register / @ObjectHolder | | |
| Blocks | 同上 | | |
| BlockEntities | | | |
| Entities | | | |
| Menus / Screens | | | |
| RecipeSerializers | | | |
| CreativeTabs | | | |
| Sounds / Particles | | | |

### 2.2 注册架构评价

- 是否使用统一的注册管理类？（如 `ModItems`、`ModBlocks` 等）
- 注册代码是否集中还是分散在多个类中？
- 是否有 `@ObjectHolder` 自动注入？
- 是否存在动态注册（运行时条件注册）？

### 2.3 关键注册代码路径

列出核心注册类的关键代码路径（类全名 + 方法名），方便魔改作者追踪。

## 3. Mixin 分析

### 3.1 Mixin 总览

| Mixin 类 | 目标类 | 注入方式 | 玩家可见影响 | 兼容风险 |
|----------|--------|----------|-------------|----------|
| | | @Inject / @Redirect / @Overwrite / @ModifyVariable / @ModifyArg | | 高/中/低 |

### 3.2 Mixin 详细分析

每个 Mixin 至少记录：
- **Mixin 类路径**：
- **目标类**：
- **注入方法/字段**：
- **注入点**：
- **修改逻辑摘要**：
- **原版行为变更**：
- **兼容风险说明**：
- **能否通过配置关闭**：

### 3.3 Mixin 风险评级

- **高风险**：@Overwrite、大范围 @Redirect、修改核心原版方法（如 `Entity#tick`）
- **中风险**：@Inject 在常用方法、@ModifyVariable 改变关键值
- **低风险**：@Inject 在边缘方法、仅添加回调

## 4. 事件系统

### 4.1 事件监听总览

| 事件类 | 监听方式 | 触发条件 | 行为摘要 | 服务端/客户端 |
|--------|----------|----------|----------|-------------|
| | @SubscribeEvent / Fabric callback | | | |

### 4.2 关键事件分析

对每个重要事件监听：
- **事件类**：
- **监听位置**：
- **触发条件**：
- **行为**：
- **玩家可见效果**：
- **是否能被其他 mod 取消/修改**：

### 4.3 事件系统评价

- 事件监听是否集中在专用类中？
- 是否使用 `EventBusSubscriber` 自动注册？
- 事件处理是否考虑性能（频率、条件判断）？
- 是否提供了自定义事件供其他 mod 扩展？

## 5. 网络包分析

### 5.1 网络通信总览

| 包类 | 方向 | 同步内容 | 使用场景 | 频率 |
|------|------|----------|----------|------|
| | C→S / S→C / 双向 | | | 高/中/低 |

### 5.2 网络协议设计评价

- 使用 SimpleChannel / CustomPayload / Fabric API 哪种方式？
- 协议版本控制是否存在？
- 数据包大小和频率是否合理？
- 是否存在安全风险（服务端未验证客户端数据）？

## 6. 配置系统

### 6.1 配置总览

| 配置项 | 类型 | 默认值 | 影响范围 | 所属类别 |
|--------|------|--------|----------|----------|
| | boolean / int / double / String / List | | 游戏玩法/客户端/服务器/平衡 | |

### 6.2 配置组织评价

- 使用 ForgeConfigSpec / Fabric Config API / 自定义 JSON / 其他？
- 配置分类是否清晰（server/common/client）？
- 配置注释是否完整？
- 运行时重载是否支持？
- 配置是否影响游戏平衡的关键参数？

### 6.3 魔改关注的配置项

重点列出整合包作者最可能调整的配置项（难度倍率、生成概率、开关控制等）。

## 7. 数据驱动内容

### 7.1 Datapack 可覆盖内容

| 内容类型 | 是否数据驱动 | 路径 | 硬编码回退 |
|----------|-------------|------|-----------|
| 配方 | 是/部分/否 | `data/<modid>/recipes/` | 有/无 |
| 战利品表 | 是/部分/否 | `data/<modid>/loot_tables/` | 有/无 |
| 标签 | 是/部分/否 | `data/<modid>/tags/` | 有/无 |
| 世界生成 | 是/部分/否 | `data/<modid>/worldgen/` | 有/无 |
| Advancement | 是/部分/否 | `data/<modid>/advancements/` | 有/无 |
| 配方类型/机器配方 | 是/部分/否 | 自定义路径 | 有/无 |

### 7.2 硬编码内容列表

列出无法通过 datapack 修改的硬编码内容。这些是魔改作者需要绕过的部分。

## 8. API 与扩展点

### 8.1 公开 API

| API 类/接口 | 用途 | 稳定性 | 文档 |
|-------------|------|--------|------|
| | | 稳定/实验性/内部 | 有/无 |

### 8.2 扩展点

- 是否提供事件供其他 mod 监听？
- 是否有 `I*` 接口供其他 mod 实现？
- 是否有能力/附件/组件系统供其他 mod 附加数据？
- 是否有 `@Plugin` 或 SPI 机制？

### 8.3 跨 Mod 集成

| 集成目标 | 方式 | 程度 |
|----------|------|------|
| JEI/REI/EMI | 内置插件 / 数据驱动 / 无 | |
| Patchouli | 内置手册 / 无 | |
| Curios/Trinkets | 支持 / 部分 / 无 | |
| Create / AE2 / 其他 | 具体集成方式 | |

## 9. 魔改指导

### 9.1 KubeJS 魔改示例

如果 mod 使用了数据驱动的配方系统，给出至少 2 个 KubeJS 魔改示例：

**示例 1：添加自定义配方**
```js
// KubeJS 脚本示例
ServerEvents.recipes(event => {
  // 添加自定义配方
  event.recipes.modid.recipeType({
    input: { item: 'minecraft:iron_ingot' },
    output: { item: 'modid:custom_item', count: 1 }
  })
})
```

**示例 2：修改机器处理时间**
```js
// 修改机器处理时间（如果支持）
```

### 9.2 CraftTweaker 魔改示例

如果 mod 提供了 CraftTweaker 集成，给出示例。

### 9.3 Datapack 魔改示例

```json
// 示例：通过 datapack 添加/覆盖配方
```

### 9.4 配置推荐

针对不同整合包类型（科技包、魔法包、冒险包、空岛），给出推荐的配置调整方案。

### 9.5 魔改限制

- 哪些内容完全不可修改？
- 哪些修改需要重启服务器？
- 哪些修改可能引发兼容问题？

## 10. 代码质量评价

### 10.1 架构质量

| 维度 | 评价 | 证据 |
|------|------|------|
| 模块化程度 | 高/中/低 | |
| 命名规范 | 清晰/一般/混乱 | |
| 注释完整度 | 完整/部分/缺乏 | |
| 异常处理 | 完善/基本/缺乏 | |
| 性能考量 | 有/部分/无 | |

### 10.2 安全与稳定性

- 服务端是否验证客户端数据包？
- 是否存在潜在的崩溃风险点？
- Mixin 是否有 fallback 机制？

### 10.3 推荐修改策略

如果要对此 mod 进行魔改，建议优先使用什么方式（datapack > 配置 > KubeJS > 核心魔改）？为什么？

## 11. 不确定项与需实测项

| 不确定项 | 原因 | 建议验证方式 |
|----------|------|-------------|
| | 混淆/动态注册/配置分支 | |
